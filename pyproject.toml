[project]
name = "noga"
version = "0.1.0"
description = "A command-line agent for searching academic papers by topic"
authors = [
  { name = "", email = "" }
]
license = { text = "MIT" }
readme = "README.md"
requires-python = ">=3.8"

[tool.python.dependencies]
# Add runtime dependencies here
